const db = require("../models");
const { Battle } = require("../models");

module.exports = {
    register: async (req, res) => {

        try {
            console.log(req.body)


        } catch (err) {
            console.log(err)
        }

    }
}